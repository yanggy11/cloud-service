server:
  port: 1982
spring:
  application:
    name: kothlin
  cloud:
    config:
      uri: http://localhost:8887
      name: kothlin # 要读取的配置文件application属性
      profile: ${config.profile:dev} # default config profile
      username: yanggy11
      password: ygy591932230
      label: cloud-service-dev
#      discovery:
#        enabled: true
#        service-id: SERVICE-CONFIG-CENTER
  zipkin:
    base-url: http://39.107.97.132:9411/
    enabled: true
    sender:
      type: rabbit
    sleuth:
      web:
        client:
          enabled: true
      sampler:
        probability: 1.0

eureka:
  instance:
    lease-renewal-interval-in-seconds: 300
    lease-expiration-duration-in-seconds: 900
  client:
    serviceUrl:
      defaultZone: http://39.107.97.132:8000/eureka/,http://47.104.89.201:8000/eureka/