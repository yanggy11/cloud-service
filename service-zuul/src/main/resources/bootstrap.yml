server:
  port: 1987
spring:
  application:
    name: zuul
  zipkin:
    base-url: http://localhost:9411/
    sender:
      type: rabbit
  sleuth:
    web:
      client:
        enabled: true
    sampler:
      probability: 1.0
  cloud:
    config:
      uri: http://39.107.97.132:8887
      name: zuul # 要读取的配置文件、application属性
      profile: ${config.profile:dev} # default config profile
      username: yanggy11
      password: ygy591932230
#      discovery:
#        enabled: true
#        service-id: SERVICE-CONFIG-CENTER
      label: cloud-service-dev
ribbon:
  ReadTimeout: 202000
  SocketTimeout: 202000
  eager-load:
    enabled: true #开启饥饿加载模式
#    clients: [] #指定需要饥饿加载的客户端名称、服务名
zuul:
  sensitive-headers:
  ignored-services: "*"
eureka:
  instance:
    lease-renewal-interval-in-seconds: 300
    lease-expiration-duration-in-seconds: 900
  client:
    serviceUrl:
      defaultZone: http://39.107.97.132:8000/eureka/,http://39.107.97.132:8001/eureka/

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 100000
        timeout:
          enabled: false

management:
  endpoints:
    web:
      expose: "*"
